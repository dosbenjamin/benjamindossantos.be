name: Deployment
on: [push]
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3
      - uses: pnpm/action-setup@2
      - run: pnpm env use --global gallium
      - run: pnpm i
      - run: pnpm lint
