<svg
  aria-hidden="true"
  class="external-link-icon"
  fill="none"
  viewBox="0 0 24 24"
  xmlns="http://www.w3.org/2000/svg"
>
  <path
    d="M7 17 17 7M7 7h10v10"
    stroke="currentColor"
    stroke-linecap="round"
    stroke-linejoin="round"
    stroke-width="2"
  ></path>
</svg>

<style>
  .external-link-icon {
    flex: none;
    width: var(--size-icon-small);
    height: var(--size-icon-small);
    color: var(--color-text-strong);
    opacity: var(--opacity-link-icon-muted);
    transition: opacity var(--motion-duration-fast) var(--motion-easing-standard);
  }

  :global(a:hover) .external-link-icon,
  :global(a:focus-visible) .external-link-icon {
    opacity: var(--opacity-link-decoration-active);
  }
</style>
